import java.util.Scanner;
public String front22(String str) {

    {

        public static String modifyWord(String word) {
        if (word.isEmpty()) {
            return word; // Возвращаем пустую строку, если входное слово пустое
        }

        char FirsttwoChar = word.charAt(word.length() - 1);
        return FirsttwoChar + word + FirsttwoChar;
    }
 }


}



}
